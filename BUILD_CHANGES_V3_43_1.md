# AnnaBot V3.43.1 — Фото снова рисуются, пак-лестница персиков, живые плитки

## 1. Студия «Картинки» снова работает
- **Seedream t2i → HTTP 422 устранён.** Freeform-запросы студии уходили на edit-эндпоинт
  `fal-ai/bytedance/seedream/v4.5/edit`, который валидирует `image_urls` как непустой
  список и отвечал 422 «Sequence should have at least 1 items». Теперь t2i ходит на
  отдельный `FAL_MODEL_T2I` (`.../v4.5/text-to-image`, переопределяется env), а пустой
  список референсов вообще не попадает в payload.
- **Gemini 429 переживает квоту.** Раньше был один ретрай — всплеск rate-limit убивал
  картинку. Теперь две повторные попытки с растущим бэкoффом (2 c → 4 c) на всех
  транзиентных статусах (408/429/5xx) и таймаутах.
- Цепочка отказа не изменилась: Seedream t2i → Gemini → OpenAI, счётчики провайдеров
  в админке «Отказы» продолжают писать причину каждого звена.

## 2. Пак-лестница персиков (10/30/100 🍑)
- Квадрат «+1 фото-кредит» заменён тремя паками, как клубничные паки у Come Closer:
  **10 🍑 = 250⭐**, **30 🍑 = 675⭐ (−10%)**, **100 🍑 = 1875⭐ (−25%)**; рублёвые и
  долларовые цены считаются из звёзд через `fiat_values`, скидки видны угловым бейджем
  на квадрате (`−10%` / `−25%`).
- Один и тот же ключ продукта (`peach_pack_10/30/100`) проходит все цепочки:
  Stars `pre_checkout`/`successful_payment`, FreeKassa notify (подтверждение
  «Пак на N персиков оплачен!»), Wallet Pay webhook и модалку способов оплаты
  (⭐ / СБП / крипта) — зачисление пака целиком в `payments.record_payment`.
- Цены переопределяются env: `PEACH_PACK_10_STARS`, `PEACH_PACK_30_STARS`,
  `PEACH_PACK_100_STARS`.

## 3. Витрина: свежие плитки и живые петли
- **Плитки-гифки пересобраны из текущих канонических фото** (`card_preview.webp` у всех
  восьми героинь): раньше Ken-Burns петля рендерилась один раз и после замены
  референсов показывала старые лица. Новый `webapp_service.rebuild_card_tile()`
  (300×400, 16 кадров, ~200–290 KB) + админ-команда `/retiles` пересобирает их на
  сервере без деплоя.
- **Живые плитки** (`/livetiles`): фоновая админ-задача прогоняет каноническое лицо
  каждой героини через i2v-цепочку (Gemini/Veo → Replicate → fal → HF) с моушеном
  «улыбается и шлёт воздушный поцелуй» (крупный план — надёжные для i2v движения) и
  сохраняет `card_live.mp4` рядом с референсами. Сетка персонажей играет его как
  muted-loop `<video>` вместо webp; пока файла нет — старая петля.
- Новый роут `GET /webapp/live/{character_id}` отдаёт mp4 с часовым кэшем.

## 4. UI-полировка по замечаниям владельца
- Баннер «Бесплатные 🍑 за подписку на канал» крупнее: паддинги 15/16, иконка 46px,
  текст 15px bold, стрелка 32px.
- Кнопка лайка на странице персонажа увеличена (16px bold, паддинги 10/16).
- Непонятная графа «СЮЖЕТ» переименована в **«КАК ВЫ ПОЗНАКОМИТЕСЬ»**
  (EN: «HOW YOU TWO MEET»).

## 5. Бренд
- `data/brand/strawberry_logo.png` — новый минималистичный логотип-клубничка
  (белая ягода на тёмном фоне) вместо старой аватарки: поставить через
  BotFather → /setphoto для @Anna67901_bot и аватар канала.

## Тесты
- `tests/test_v3431_release_static.py` — 9 статик-пинов релиза (эндпоинт t2i,
  ретраи 429, цены/гранты/цепочки паков, пересборка плиток, живые плитки, UI).
- Обновлены пины: test_v3195 (range(3) + бэкoфф), test_v3340 (паки вместо
  одиночного кредита, whitelist `packBadge(x)`/`cardMedia(c)`), test_v3360
  (fiat-пары паков), test_v3390 (новая подпись графы).
- Полный набор: **656 passed**.
